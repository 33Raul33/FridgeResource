# ── Desarrollo local ──────────────────────────────────────────────────────────
# Obtén tu clave gratuita en: https://aistudio.google.com/app/apikey
# Copia este archivo como .env.local y rellena tu clave

# ── Vercel (producción) ───────────────────────────────────────────────────────
# Añade GEMINI_API_KEY en el dashboard de Vercel:
# Settings → Environment Variables → GEMINI_API_KEY
# La clave NUNCA debe aparecer en el código fuente ni en el bundle del browser.

# ── Desarrollo con Vercel CLI (con serverless local) ─────────────────────────
# npx vercel dev   → requiere el .env.local anterior
